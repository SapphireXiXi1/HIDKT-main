# =============================================================================
# Tested with Python 3.9. Recommended: use a virtual env or conda env.
# =============================================================================

# -----------------------------------------------------------------------------
# [REQUIRED] Core — training & evaluation will not run without these
# -----------------------------------------------------------------------------
torch==2.5.1
numpy==1.26.4
scikit-learn>=1.3.0,<2.0

# -----------------------------------------------------------------------------
# [REQUIRED indirectly] scikit-learn runtime deps (usually auto-installed)
# If pip does not pull them in, install manually:
# -----------------------------------------------------------------------------
scipy>=1.10.0
joblib>=1.2.0
threadpoolctl>=3.0.0

# -----------------------------------------------------------------------------
# [OPTIONAL] GPU acceleration
# CPU-only: `pip install torch` is enough.
# CUDA 12.x (example, match your driver / conda export):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
# Or conda: pytorch + pytorch-cuda=12.4
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# [NOT REQUIRED] Listed in a full conda export but unused by P0–P6 training code
# Safe to skip unless you add your own analysis / plotting scripts:
# -----------------------------------------------------------------------------
# matplotlib==3.9.2
# pandas==2.2.2
# seaborn==0.13.2
# openpyxl==3.1.5
# tqdm==4.66.5
# PyYAML==6.0.2
# requests==2.32.3
# pyqt / qt — only if you use Qt-based GUI tools
# -----------------------------------------------------------------------------
# Conda/system packages (do NOT pip install): cuda-*, mkl, intel-openmp, vc,
# brotli, openssl, etc. Install PyTorch with CUDA via official PyTorch/conda docs.
